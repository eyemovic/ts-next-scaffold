{
  "packages": ["bun@1.0.22"],
  "shell": {
    "init_hook": ["echo 'Welcome to devbox!' > /dev/null"],
    "scripts": {
      "start": "cd app && bun run dev",
      "test": ["echo \"Error: no test specified\" && exit 1"]
    }
  }
}
